version: 2.1
jobs:
  build:
    docker:
      - image: amazonlinux:2
    steps:
      - checkout
      - run:
          name: make
          command: |
            yum install make_4.1-9.1_arm64.deb
            make all